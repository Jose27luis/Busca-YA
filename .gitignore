# Core de MediaWiki instalado localmente (no se versiona: ~444M + secretos en LocalSettings.php)
/mediawiki/

# Prototipo de interfaces extraído (el fuente versionado es interfaces.zip)
/interfaces/

# Servicio de embeddings (venv y modelos descargados)
/embeddings/venv/
/embeddings/.hf/
/embeddings/__pycache__/
